def str_method_exampl():
    s = "If you add a USB controller to a virtual machine in the VMware Host Client version 1.8, you cannot remove the USB controller."
    print("s = ", s)
    print(s.count('o'))
    #'O' + s[2:]
    #print(s)
    if (s.isalpha() == False):
        print(s.upper())
    if len(s) > 4:
        print(s.lower)
    'O' + s[1:]
    print(s)
   # c = len(s)
    #k = 0
    #v = s.find('o',k,c)
    #print(s.find('o',k,c))
    #k = v
